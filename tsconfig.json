{
  "compilerOptions": {
    "target": "ESNext",                      // Modern JavaScript target.
    "lib": ["DOM", "DOM.Iterable", "ESNext"], // Ensure compatibility with browser and modern features.
    "module": "ESNext",                      // Use ES module syntax for modern JavaScript.
    "moduleResolution": "Node",             // Resolve modules in a Node.js environment.
    "skipLibCheck": true,                   // Speeds up compilation by skipping library type checks.
    "resolveJsonModule": true,              // Allow importing JSON files as modules.
    "isolatedModules": true,                // Ensures each file is treated as a separate module.
    "jsx": "react-jsx",                     // Enable React JSX transformation.

    /* Linting and Safety */
    "strict": true,                         // Enable strict type-checking options.
    "noUnusedLocals": true,                 // Report unused local variables.
    "noUnusedParameters": true,             // Report unused function parameters.
    "noFallthroughCasesInSwitch": true,     // Ensure all switch cases are handled.

    /* Interop and ES Module Support */
    "esModuleInterop": true,                // Ensures compatibility with CommonJS modules.
    "allowSyntheticDefaultImports": true,   // Enable default imports for CommonJS modules.
  },
  "include": ["src"],                        // Specify included files for compilation.
  "references": [
    { "path": "./tsconfig.node.json" }      // Ensure this file exists if references are used.
  ]
}
